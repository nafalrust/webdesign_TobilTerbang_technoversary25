version: '3.8'

services:
  backend:
    build: .
    container_name: technoversary-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    volumes:
      - ./app:/app/app
    restart: unless-stopped
